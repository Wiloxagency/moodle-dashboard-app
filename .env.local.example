# Local Environment Configuration (Copy to .env.local for local overrides)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_ENVIRONMENT=development

# You can override any environment variable here for local development
# This file should be copied to .env.local and customized as needed
